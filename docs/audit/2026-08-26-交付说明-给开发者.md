# 交付说明（给开发者）

本次工作基于 `master`（v6.0.45, `37eff86`），产出 4 个提交。全部在本地分支
`fix/toc-empty-onclick-bookurl`，**未推送远端**。

## 提交一览

| 提交 | 主题 | 规模 |
|---|---|---|
| `bcbbbbd` | 修「搜索正常但点进书籍无目录」（生产反馈） | 4 文件 |
| `32b4a82` | 全量对抗性安全审计修复（2 Critical + 9 High + 15 Medium + 12 Low） | 17 文件 |
| `4e29c0d` | EPUB/PDF 解析优化（借鉴 komga-cn riir-dev） | 2 文件 + 文档 |
| `4210035` | 本地书章节命名空间回填迁移（修上条对存量数据的不完整） | 1 文件 |

**测试**：`cargo test` 全绿（756 lib + 14 e2e）。3 个需真实 DNS 的 SSRF 测试在离线环境
2 秒超时自动跳过，有网时正常执行。

## 建议的审阅顺序

1. **先看 `bcbbbbd`**：最小、最贴近生产反馈，理解「书源规则静默失败」这条主线。
2. **再看 `32b4a82` 的 Critical/High 部分**：安全影响最大，改动点集中在
   `opds.rs`（路径穿越）、`router.rs`（MongoDB 越权、静态服务鉴权、静默失败簇）、
   `crawler.rs`（SSRF/响应体上限）、`search.rs`（panic 守卫）。
3. **`4e29c0d` + `4210035`** 可一起看：EPUB/PDF 解析质量 + 配套数据迁移。

详细清单见同目录：
- `2026-08-26-对抗性安全审计报告.md` —— 38 项发现，每项含 file:line、复现场景、修复建议、核实程度
- `2026-08-26-审计修复总结.md` —— 逐项修复说明 + 端到端验证结果
- `2026-08-26-EPUB-PDF解析优化.md` —— 5 项优化的动机/改法/取舍
- `2026-08-26-komga对比-EPUB-PDF借鉴.md` —— komga-cn 调研原始结论

## 行为变更（需要留意）

这几处**改变了对外可观察行为**，升级时请确认符合预期：

1. **目录/正文/详情解析为空 → 明确业务错误**（此前返回 `isSuccess:true` + 空数据）。
   前端若依赖「成功但空」来展示空态，需同步调整；好处是用户能看到真实错误而非空白。
2. **secure 模式下的收紧**（非 secure 单用户模式行为不变）：
   - MongoDB 备份/恢复需管理员/secureKey；
   - `/epub`、`/book-assets` 需 token 且 URL 首段命名空间必须匹配；
   - `getSystemInfo` / `getServerStats` 需登录。
3. **爬虫新增 `READER_HTTP_MAX_BYTES`**（缺省 32 MiB）：响应体超限报错。若有书源返回
   超大响应，需调大该值。
4. **多源搜索单源超时 25 秒**：极慢书源不再拖垮整批搜索，但其结果会缺失。
5. **正文分页上限 5 → 20**：超长分页章节不再被静默截断。

## 数据迁移

`4210035` 会在启动时执行一次性回填（`backfill_chapter_namespace`）：把
`book_chapters` 中 `user_namespace='default'` 的行，按 `books` 表归属改写为真实用户命名空间。
**仅回填「恰好属于唯一一个非 default 命名空间」的书**；共享书（多用户收藏同一 book_url）
与本就属于 default 的公共书保持不动。幂等，重复启动影响 0 行。

另：`migrate_admin_default_personal_data_back` 改为一次性执行（`system_settings` 标记），
此前每次启动都会搬移/删除 default 下的个人数据行。

## 已知限制 / 未做

1. **`/assets` 静态目录仍未鉴权**：UI 上传的背景图经 CSS `url()` 加载，无法携带 token，
   强制鉴权会破坏功能。书籍正文（更敏感）已由 `/epub`、`/book-assets` 的命名空间校验覆盖。
   彻底方案需签名 URL，改动较大。
2. **源书正文缓存无 TTL / 不识别「坏数据」**：空正文已不再缓存；但非空的风控页/占位文本
   仍会被缓存。识别「坏」易误伤合法短章节，且加 TTL 需给 `book_chapters` 加列（schema 迁移），
   留待产品定义判据。
3. **PDF 封面仅支持内嵌 JPEG/JP2**：纯 lopdf 方案，零新增依赖，对扫描版 PDF 有效；
   矢量排版页无内嵌图则无封面。升级为整页渲染需引入 pdfium 原生库（见 EPUB/PDF 文档）。
4. **EPUB3 扩展元数据未接**：作者角色、系列、阅读方向、landmarks 等 komga 有的字段，
   reader-dev 的 `OpfMeta` 无对应字段，需先扩数据模型与前端展示。
5. **重定向仍有 SSRF rebinding 残余**：入口 host 已钉死校验过的 IP，但 reqwest 内部发起的
   重定向连接无法逐跳钉 IP（已在 `ssrf_redirect_policy` 注释说明），每跳仍做公网校验。

## 应用方式

见 `patches/如何应用.txt`。推荐 `git bundle`（带完整提交信息）：

```
git fetch /path/to/reader-dev-fixes.bundle fix/toc-empty-onclick-bookurl:fix-audit
git log --oneline master..fix-audit    # 确认 4 个提交
git merge fix-audit
```

注：部分 git 版本（实测 Apple Git 2.50.1）对本组 `.patch` 的 `git am` 会误报冲突，
但 `git apply` 正常、diff 本身无误。bundle 方式不受影响。
